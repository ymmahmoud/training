const getChecklistTemplates = async () => {
    // SELECT text, active, credentials.name from checklistItems LEFT JOIN credentials ON  credentials.id = checklistItems.credentialId GROUP BY credentialId;
    // TODO IMPLEMENT
};